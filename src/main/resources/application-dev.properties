# Oracle dev 서버용 프로파일 — 접속 정보·키 값은 서버의 .env(docker --env-file)가 공급한다 (deploy/dev/ 참고)
# 베이스(application.properties)와의 차이만 정의한다.

# dev는 디버깅 편의를 위해 INFO 로그
logging.level.root=INFO

# dev는 부하테스트(k6) 대상이라 prod와 동일한 기동 특성 유지 — 나머지는 베이스 설정을 그대로 따른다
